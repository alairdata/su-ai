import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build', (WidgetTester tester) async {
    // Basic smoke test — full widget tests added per feature
    expect(true, isTrue);
  });
}
